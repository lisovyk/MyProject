done: 
plotly

to do:
# modal if NAs are in table
! bug: rhandson deletes not used rows from user table, which it should not
!urgent Last column not coercing to numeric if only one numeric is in it [e.g. svg's in specdata]

на графс табі нові контроли для графс
bug: crash on 2 columns left
dropdown:
plotly 
highcharts

bug: upload bugged, multiple uploads

questions:






"$('a.active').attr('href') == '#second_tab'")