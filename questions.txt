done: 
plotly

to do:
highcharter color support
! highcharter (dropdown with plotly/highcharter choices, responsive UI)
! bug: rhandson deletes not used cols from user table, which it should not
! Last column not coercing to numeric if only one numeric is in it [e.g. svg's in specdata]
rewrite convert.types(causes error if you delete up to 2 cols left and then get them back; probably will fix itself with delete-cols bug
на графс табі нові контроли для графс
? modal if NAs are in table


bug: crash on 2 columns left


bug: upload bugged, multiple uploads

questions:



"$('a.active').attr('href') == '#second_tab'")